<!-- Loop over each object in the projects array -->
{{#each userPosts as  |posts| }}
<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      {{posts.title}}
        <h6>     {{posts.snippet}}</h6>
    </h2>
    
</div>
{{/each}}